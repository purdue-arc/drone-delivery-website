{
  "name": "drone-delivery-website",
  "private": true,
  "scripts": {
    "dev": "npm run dev --workspaces --if-present"
  },
  "gitHooks": {
    "pre-commit": "eslint --ext .tsx,.ts src/"
  },
  "engines": {
    "node": ">=16.8"
  }
}
