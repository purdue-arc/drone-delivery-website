{
  "name": "drone-delivery-website",
  "private": true,
  "scripts": {
    "dev": "pnpm --recursive run dev"
  },
  "gitHooks": {
    "pre-commit": "eslint --ext .tsx,.ts src/"
  },
  "engines": {
    "node": ">=16.8"
  }
}
